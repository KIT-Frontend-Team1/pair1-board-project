import styled from "styled-components";
const Button = () => {
  return <Wrapper>POST</Wrapper>;
};

const Wrapper = styled.button`
  width: 100px;
  height: 50px;
  border-radius: 25px;
`;

export default Button;
